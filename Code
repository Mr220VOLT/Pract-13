sales = {"Латте":15, "Каппучино":12, "Эспрессо":30,
         "Руссиано":1, "Американо":13, "Доппио":25}
prices = 0
print('Введи название напитка из перечисленных и количество грамм (пример Латте, 17):\n' + 
    ", ".join(sales.keys()))
while True:
    try:
        choice = input() 
        if choice == "calc": 
            break
        else:
            temp = choice.split(", ")
            prices += sales[temp[0]] * int(temp[1])
            print("Введите следующую покупку и колличество грамм или введите calc для завершения покупки...")
    except KeyError:
        print("Извините, такого напитка не существует, попробуйте снова.")
print('Ваша покупка обошлась вам в:', str(prices) + "$")
